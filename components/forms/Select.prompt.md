Native select restyled with a chevron. Set `dark` for the company switcher on the header bar.

```jsx
<Select options={['Todas as empresas', 'Auto Posto Nova Açailândia']} dark />
<Select label="Itens por página" options={['5','10','25']} />
```
