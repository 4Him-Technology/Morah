The base white surface used for every panel and list item.

```jsx
<Card>…</Card>
<Card interactive padding="var(--space-4)">…</Card>
```
Set `interactive` for list/grid cards that should lift on hover.
